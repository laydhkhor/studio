'use client';

/**
 * This component has been deprecated as custom error handling was removed.
 * It remains as an empty fragment to prevent import errors during transition.
 */
export function FirebaseErrorListener() {
  return null;
}
