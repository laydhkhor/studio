rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV MODE: Allow all read/write access for rapid prototyping
    // IMPORTANT: Restrict these rules before moving to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}