rules_version = '2';

// FORCED REDEPLOY: Standard Development Rules
// Updated: 2025-02-14 (Reset to allow-all for development)
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}