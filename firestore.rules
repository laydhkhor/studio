rules_version = '2';

// Standard Development Rules
// Last Synced: 2024-07-26 (Forced redeploy to resolve permission errors)
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}